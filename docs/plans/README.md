# Implementation plans

Use `active/<issue>-<slug>.md` for non-trivial work. Required sections are
context, goals, non-goals, decisions, interfaces, steps, tests, risks, current
state, next step, verification, and completion summary.

Move the document to `completed/` after merge. Preserve it as implementation
and verification evidence; GitHub issues remain the task-status source.
