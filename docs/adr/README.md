# Architecture decision records

ADRs are numbered, immutable records of durable decisions. Use status
`proposed`, `accepted`, `deprecated`, or `superseded`. A changed decision gets a
new ADR that names the record it supersedes.
