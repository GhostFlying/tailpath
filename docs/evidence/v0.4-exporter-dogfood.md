# v0.4 embedded exporter dogfood ledger

Issue: [#122](https://github.com/GhostFlying/tailpath/issues/122)

Status: passed on 2026-08-28

## Qualified artifact

- Main commit: `0357ac8009f1f6c51326ff2b737179cb19b95799`
- Image: `ghcr.io/ghostflying/tailpath:edge-0357ac8009f1f6c51326ff2b737179cb19b95799`
- OCI index digest:
  `sha256:26f16e83b21f017689ae90d2bfeb876d448bca13ef5a470e7d6fad071bd26838`
- Image-reported version: `edge-0357ac8009f1`
- Main workflow:
  [33149461397](https://github.com/GhostFlying/tailpath/actions/runs/33149461397)
- Platform: Linux amd64, kernel `5.15.120.bsk.3-amd64`, Docker `20.10.24`,
  Compose `2.39.2`, and Tailscale `1.102.2`

The workflow completed the repository checks, native collector matrix,
archives, image gate, immutable multi-architecture publication, and mutable
edge promotion before enrollment began. The dogfood host pulled the full-SHA
tag and resolved the digest above; no local rebuild, PR image, short-SHA tag,
or mutable `edge` tag was used for qualification.

## Sanitized results

| Scenario | Runtime state | Business path | Traffic and History | Result |
| --- | --- | --- | --- | --- |
| Enrollment | 3 known / 3 reporting / 0 stale | Direct | Dedicated reporter was not an observer | Pass |
| Direct baseline | 3 / 3 / 0 | Direct, active | Both directions positive; two observations; History non-empty | Pass |
| UDP constrained | 3 / 3 / 0 | DERP, active | Both directions positive; Direct and DERP retained in History | Pass |
| UDP restored | 3 / 3 / 0 | Direct, active | Both directions positive; Direct -> DERP -> Direct retained | Pass |
| Server outage | 3 / 3 / 0 after recovery | Direct, active | One degraded/recovered episode; canonical edge stable; rate and post-outage History growth within no-catch-up bounds | Pass |
| Runtime withdrawal | 3 known / 2 reporting / 1 stale | Direct, active | Unrelated bidirectional workload stayed active | Pass |
| Runtime rejoin | 3 / 3 / 0 | Direct, active | Withdrawn runtime kept its private canonical identity | Pass |
| Exporter restart | 3 / 3 / 0 | Direct, active | Canonical edge and History preserved; no catch-up spike | Pass |

Every business-edge capture reported two endpoint observations,
`systemTelemetry=false`, positive traffic in both directions, and zero clock
warnings. The reporter was never counted as an observer. Its ordinary control
traffic is intentionally not part of this workload, so system-telemetry
classification and default hiding remain covered by the deterministic
cross-layer and browser gates.

The first immutable candidate exposed a real reconnect-baseline bug and an
incorrect sliding History comparison. That candidate was not qualified. The
fixes were reviewed in [#136](https://github.com/GhostFlying/tailpath/pull/136),
merged to the commit above, and the complete qualification was repeated from a
clean Compose project and new evidence directory.

## Privacy and cleanup

- The reusable key was mounted from a private file, zeroed immediately after
  enrollment, and remained zero bytes through cleanup.
- UDP filtering existed only in the disposable exporter network namespace and
  was restored before teardown.
- The server outage produced exactly one degraded and one recovered transport
  transition. Log scanning found no credential, endpoint, stable identity, or
  disco identity value. Expected generated tsnet hostnames appeared only in
  private startup logs, which were deleted.
- All containers, named volumes, and networks owned by the qualification
  project were removed. All raw topology, History, and log files were purged.
- The retained sanitized summaries were rescanned and contain no credentials,
  hostnames, Tailnet suffixes, node identities, or network endpoints.

Tags and releases remain human-only.
