# v0.4 embedded exporter dogfood ledger

Issue: [#122](https://github.com/GhostFlying/tailpath/issues/122)

Status: pending immutable `main` artifact

The dogfood tooling must merge before CI can publish an immutable image that
contains both the Tailpath server and multi-instance tsnet example. This ledger
will be completed from fail-closed sanitized evidence during v0.4 closeout. It
must never contain raw topology, Tailnet names, node identities, endpoints,
credentials, or unsanitized logs.

Tags and releases remain human-only.
