# v0.5 Devices API dogfood ledger

Issue: [#153](https://github.com/GhostFlying/tailpath/issues/153)

Status: pending

This ledger is intentionally incomplete until the full v0.5 stack is merged,
main publishes its immutable full-SHA image, and a user-created OAuth client
with exactly `devices:core:read` completes the real qualification run.

## Qualified artifact

- Main commit: pending
- Image: pending
- OCI index digest: pending
- Main workflow: pending
- Generic platform versions: pending

## Sanitized results

| Scenario | Required retained evidence | Result |
| --- | --- | --- |
| First sync | Duration, counts, Live-isolation booleans, identity/content hashes | Pending |
| Server restart | Status, counts, hash comparison, Live counts | Pending |
| Invalid secret | Fixed error category, last-good boolean, counts, identity/content hashes | Pending |
| Recovery | Duration, status, Live-isolation booleans | Pending |
| Token renewal | Run duration, success advancement, status, counts, hashes | Pending |

The final ledger must contain no device name, MagicDNS name, hostname, IP,
tag, NodeKey, StableNodeID, canonical ID, Tailnet name, credential, raw
timestamp, raw JSON, or log payload. The directory represents only devices
visible to the tested credential and is not a Tailnet completeness assertion.

## Closeout

- Immutable main image and successful required CI: pending
- Existing 250-node / 1,000-edge scale workflow: pending
- 250-device workspace gate: passed locally in implementation, hosted run pending
- Real OAuth run longer than 65 minutes: pending
- Independent read-only blocker review: pending
- Private raw evidence deleted and OAuth client revoked: pending

Tags and releases remain human-only.
