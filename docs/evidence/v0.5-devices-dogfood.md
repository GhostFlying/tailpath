# v0.5 Devices API dogfood ledger

Issue: [#153](https://github.com/GhostFlying/tailpath/issues/153)

Status: passed on 2026-09-05

This ledger contains only sanitized results. After independent review, the
isolated deployment and private mode-0700 directory containing raw directory
responses, topology, screenshots, logs, node identities, network metadata, and
credentials were deleted.

## Qualified artifact

- Main commit: `01d09b8aac49941beed291aaa97bec1ac637c1d7`
- Image:
  `ghcr.io/ghostflying/tailpath:edge-01d09b8aac49941beed291aaa97bec1ac637c1d7`
- OCI index digest:
  `sha256:c279a894d3a07ccc6a00773139ea80277ad3844a3fbfcba29d4b87ec99b85043`
- Image-reported version: `edge-01d09b8aac49`
- Main workflow:
  [33965967635](https://github.com/GhostFlying/tailpath/actions/runs/33965967635)
- Scale workflow:
  [33966828491](https://github.com/GhostFlying/tailpath/actions/runs/33966828491)
- Platform: Linux amd64, kernel `5.15.120.bsk.3-amd64`, Docker `20.10.24`,
  Compose `2.39.2`, and Tailscale `1.102.2`

The required main workflow passed repository checks, all three native
collector jobs, collector archives, image verification, immutable amd64/arm64
publication, and edge promotion. The dogfood server used the full-SHA tag and
OCI index above; no local rebuild, PR image, short-SHA tag, or mutable `edge`
tag was used for qualification.

## Sanitized results

| Scenario | Duration | Directory / runtime state | Live isolation | Result |
| --- | ---: | --- | --- | --- |
| First sync | 4 s | Healthy; 3 directory devices; 2 with runtime evidence; 1 directory-only | 0 nodes / 0 edges; directory-only absent | Pass |
| Server restart | 16 s | Healthy; identity set and canonical mapping stable | Counts and directory-only isolation unchanged | Pass |
| Invalid secret | 10 s | Stale; `unauthorized`; all 3 last-good entries and hashes preserved | Counts unchanged | Pass |
| Recovery | 7 s | Healthy; canonical mapping stable | Directory-only isolation preserved | Pass |
| Token renewal | 4,218 s | Healthy; success advanced 4,241 s; same server process; 0 container restarts; identity set and canonical mapping stable | Directory-only isolation preserved | Pass |

Successful snapshots carry a new directory collection time, which is included
in the sanitized content hash. A changed content hash therefore confirms a new
full snapshot but does not by itself prove semantic metadata drift. Runtime
conflicts are not content-hash inputs. The identity-set and canonical-mapping
hashes remained stable. The invalid-secret snapshot retained the same identity,
directory-authoritative content (including its original collection time), and
canonical-mapping hashes as its healthy baseline, with
`lastGoodPreserved=true`.

The first sync produced identity-set hash
`02415c26aed48d7b81d9a907cc38217fa508c472156bc063dfe5f0093d441213` and
canonical-mapping hash
`06b5ecb0b9f037b4f5d9d46ebdab0d5eb21588d1ccfd8f02bc5afa5c559f7481`.
The stale baseline and result both produced directory content hash
`174ca40d5eaf84448e51eef8af27cd35823533f19a172493319b88557c0b27ba`.
The renewal result kept the same identity-set and canonical-mapping hashes. Its
directory content hash changed from
`7d3f59a0f08add14b3524399fac22daa1461247a29b3de32772d62eafc22c2a9` to
`20e79865dc48bd4281bf09102ff5e4f62981c89ffa55fed0adfcbad25e3c9228`.
At minimum, the later collection time explains this change; no semantic
metadata change is inferred from the hash alone. Private process-identity
comparison confirmed the renewal used the same server process with a Docker
restart count of zero; only those booleans are retained publicly.

## UI and scale

- Real Devices UI checks passed at 1440x900, 390x844, and 320x700. Each view
  rendered all 3 entries without horizontal overflow or console errors,
  preserved separate control and runtime dimensions, and completed its
  desktop or mobile-detail interaction.
- Copy actions returned a value exactly equal to the API MagicDNS value in all
  three viewports. The real response contained no trailing-dot input; the
  trailing-dot branch remains covered by the hosted fixture browser test.
- The 10-minute constrained gate accepted 75,000 of 75,000 reports at 125
  reports/s with zero rejected receipts, HTTP failures, 500s, request errors,
  restarts, or OOMs. Ingest p95 was 61.517 ms, p99 was 84.483 ms, and peak RSS
  was 64,256 KiB.
- Topology p95 was 10.662 ms, History list p95 was 196.810 ms, and History
  detail p95 was 8.935 ms. The seven-day 250-node / 1,000-edge database was
  707,952,640 bytes.
- The 250-node / 1,000-edge browser fixture was ready in 2,718 ms on desktop
  and 2,919 ms on mobile; desktop SSE-to-visible update was 357 ms. The
  1,000-session Peer Relay fixture was ready in 3,008 ms on desktop and 2,856
  ms on mobile. All four browser captures had zero console errors.
- Scale artifact `9969907597` retains performance, restart, history-size,
  cgroup, container, and desktop/mobile browser evidence through 2026-10-05.
- The final canonical `make check` passed all Go checks and tests, 63 Web unit
  tests, production build, and 45 Playwright cases with 15
  project-conditional skips.

## Privacy and closeout

- The enrollment key was zeroed after enrollment; its private file was deleted
  during lab cleanup.
- The OAuth secret was mounted only from the private directory and was absent
  from API errors, server logs, and sanitized summaries.
- Twenty-six private identity, network, and credential patterns extracted from
  the raw captures matched none of the retained sanitized summaries.
- The optional directory represented only devices visible to the tested
  credential. Device count and metadata changes are not interpreted as a
  complete Tailnet inventory.
- The more-than-65-minute renewal gate passed, and final independent blocker
  review found no remaining P0/P1/P2 findings.
- The isolated containers, volumes, networks, OAuth secret file, and private raw
  evidence were deleted after review. External OAuth client and reusable
  enrollment-key revocation remain operator actions.

Tags and releases remain human-only.
